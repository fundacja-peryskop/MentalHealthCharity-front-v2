/**
 * App-wide brand/chrome content shared across views (auth shell, headers,
 * footers). View-specific copy lives in each view's own content module.
 */

export const brand = {
    name: "Fundacja Peryskop",
    shortName: "Peryskop",
} as const;
